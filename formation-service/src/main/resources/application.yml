server:
  port: 8082

spring:
  application:
    name: formation-service
  datasource:
    url: jdbc:postgresql://localhost:5432/db_formation
    username: postgres
    password: mon_mot_de_passe
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    # AJOUT CRUCIAL : Contourne le blocage réseau Windows en utilisant l'adresse IP
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
