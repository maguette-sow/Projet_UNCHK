server:
  port: 8084

spring:
  application:
    name: budget-service
  datasource:
    url: jdbc:postgresql://localhost:5432/db_budget
    username: postgres
    password: mon_mot_de_passe
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    # AJOUT CRUCIAL : Résout le blocage de l'initialisation Eureka sous Windows
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
