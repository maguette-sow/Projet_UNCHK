server:
  port: 8085

spring:
  application:
    name: insertion-service
  datasource:
    url: jdbc:postgresql://localhost:5432/db_insertion
    username: postgres
    password: mon_mot_de_passe
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
